name: Tooltip
description: >
  A tooltip component that applies Bootstrap tooltips to specified elements.

properties:
  tooltip_selector:
    type: string
    description: CSS selector for the elements that will have tooltips applied.
    default: "[data-bs-toggle='tooltip']"

default:
  tooltip_selector: "[data-bs-toggle='tooltip']"
